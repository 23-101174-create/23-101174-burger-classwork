import React, { Component } from 'react';
import "./Subtitle.css"

const Subtitle = (props) => {
    return ( <>
        <div className={props.css3} > {props.text3} </div>
    </> );
}
 
export default Subtitle;